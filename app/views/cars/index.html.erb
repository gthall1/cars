<h1> All Cars </h1>

<ul>
  <% @cars.each do |car| %>
    <li><%= link_to(car.make.name + " " + car.year, car_path(car)) %></li>
  <% end %>
</ul>

<%= link_to 'Add new car', new_car_path %><br><br>

<%= link_to 'See all Manufacturers', makes_path %>
